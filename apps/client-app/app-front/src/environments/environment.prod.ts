import { IEnvironment } from "src/app/interfaces/environment-interface";

export const environment: IEnvironment = {
    production: true,
    url: 'http://localhost:8000/api' // TODO Change later after prod set up
}